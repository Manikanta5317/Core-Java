class F 
{
	static int a;//attribute or field or property or globel variable
	//a is not member of main method,methods but it can be used through out the method
	//not only main method but also used in another method
	//all global variable automatically intialization the default value
	//intial(default) value depend on  its data type or default values depend on datatype
	public static void main(String[] args) 
	{
		System.out.println(a);
	}
}
